# Vue CLI 5.0.9 — WSC2026 starter app

This repo is a Vue project **generated by Vue CLI (@vue/cli) 5.0.9** (the WSC2026-pinned
scaffolder), served with vue-cli-service.

```bash
docker compose up --build
```

Open **http://localhost**. Pinned: Node 24.1.0 / npm 11.5.0, scaffolder @vue/cli 5.0.9.

First start compiles the app (webpack), so give it a few seconds.
